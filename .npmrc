# Hoist nothing surprising; keep workspace protocol strict so eject is honest.
auto-install-peers=true
strict-peer-dependencies=false
# Astro + Tailwind v4 need these accessible; keep the store deterministic.
shamefully-hoist=false
